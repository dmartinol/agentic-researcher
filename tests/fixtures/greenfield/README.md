# Greenfield fixture

Represents a research workspace before setup.

Expected behavior:

- classify as greenfield;
- create/adopt RESEARCH.md, STATE.md, and research/memory only as part of the approved local workflow;
- resolve logical subsystem configuration before external mutation;
- require approval before provider-backed initialization;
- make repeated initialization discover/reuse already-created resources.
