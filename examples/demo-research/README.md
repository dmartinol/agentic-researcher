# Demo Research Scenario

This directory contains a repeatable scenario contract for exercising `agentic-researcher`.

It intentionally contains no completed findings. A demo generates evidence and memory from live sources so provenance, freshness, contradiction handling, and resumption are observable.

See `scenario.md` and `../../docs/demo-script.md`.
